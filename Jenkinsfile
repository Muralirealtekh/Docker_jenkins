pipeline{
    agent{
        docker{
            image 'ubuntu:latest'
        }
    }
    stages{
        stage("build"){
            steps{
                sh 'uname -a'
            }
        }
        stage("deploy"){
            steps{
                sh 'uname -a'
            }
        }
    }
}
